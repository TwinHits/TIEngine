class SceneObject():
	An object that can be drawn
	sendMessage(msg): Every object can create and send messages. everyone = 0,
		target = id
	receiveMessage(msg): Every object can recieve messages every frame.
		When an object must do a thing, it puts the thing into it's queue.
		Each update, it looks at each thing in it's queue and checks if its
			requirements are filled.
		If so, it removes it from the queue.
		This lets the object do more than one thing at once.
		Individual commands are expected to know their dependencies on other things.

class Message(): A vehicle for sending messages between objects.
	SenderId: The object that sends the message.
	recieverId: The targer object that recieves the message. If =, then all

class Player(): A player object.
