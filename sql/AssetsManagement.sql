CREATE TABLE TIE_Images (
	imageId serial PRIMARY KEY NOT NULL,
	imagePath text             NOT NULL);

CREATE TABLE TIE_Audio (
	imageId serial PRIMARY KEY NOT NULL,
	imagePath text             NOT NULL);
