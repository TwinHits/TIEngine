Convert devConsole history items into one text component
LogManager should print the name of the class that called it.
Common error messages should be constants
Scroll zone does not update with window size change
LogManager should default to a crash log to prevent circular dependencies

ConsoleManager Scrolling Behavior
	Should scroll within the devConsole window only
	Should not scroll the other engine layer elements
	Should scoll on page up and page down

Move Some TIEntity Factory or Lua Component Reading to the ComponentSystem
