SceneManager:
	Responsible for deciding which objects are to be drawn.
	draw(camera):
		Optimally draws objects.

ObjectManager:
	Responsible for storing and destroying objects.

ConfigManager:
	Responsibile for loading and paring configuration files.

AssetsManager:
	returns asset refernce when requested by type.

LogManager:
	Responsible for outputting data into the log files. Can designate different
		logging types and levels.
	Responsible for ouputting a 'crashlog' for any errors before log is
		established.
	FIle value keys should be case insensitive
